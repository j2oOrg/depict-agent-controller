# Depict Agent Controller

Static mock website for the Depict e-commerce agent controller concept.

Cloudflare Pages is configured from the `main` branch with no build command and `/` as the output directory.
